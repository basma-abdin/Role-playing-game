#include <SFML/Graphics.hpp>

const int w = sf::VideoMode::getDesktopMode().width;
const int h = sf::VideoMode::getDesktopMode().height;
const float heightScale (h/62);
const float widthScale ((w - 100)/100);
